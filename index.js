const participants = ["Rafael", "Manoel", "Daniel"]
function positions(firstPlace, secondPlace, lastPlace) {

    firstPlace = participants[0]
    secondPlace = participants[2]
    lastPlace = participants[1]
    console.log(`
The first runner is ${firstPlace}
The second runner is ${secondPlace}
The last runner is ${lastPlace}`)
}

positions()